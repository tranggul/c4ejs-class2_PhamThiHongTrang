ex1:
a.
-Var and const: ways for variable declarations.
b.
-different between let and var:
+variable that is declared with var outside a function block is available for use in the whole window, can be re-declared.
+variable declared in a block with the let is only available for use within that block, can be not re-declared.
c.
-different between let and const:
+const can not be updated and re-declared.
+let can be upadated and re-declared.
d.
let and const: declarations can only be accessed within the block it was declared.
var: everywhere.
ex2:
a. boolean is a primitive data type commonly used in computer programming languages.
b. have 2 types of result: true and false.


